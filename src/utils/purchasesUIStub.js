/**
 * Stub for react-native-purchases-ui when the package is not installed.
 * Exports a no-op so require('react-native-purchases-ui') resolves without throwing.
 */
module.exports = { default: null };
